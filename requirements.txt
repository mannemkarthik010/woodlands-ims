# Local development. Everything here is pure-Python or ships wheels
# everywhere, so `pip install -r requirements.txt` cannot fail on a
# missing compiler. Production extras live in requirements-prod.txt.
Django>=5.0,<6.0
python-dotenv>=1.0
django-simple-history>=3.5
