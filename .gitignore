*.pyc
__pycache__/
.env
.venv/
venv/
db.sqlite3
/media/
/staticfiles/
.DS_Store

# Editor / session files
.Rhistory
.Rapp.history
.vscode/
.idea/

# Client data — real menu, sales exports, the kitchen's own recipes.
# None of this belongs in the repository; the recipes in particular are the
# restaurant's own, and they stay on the machine that needs them.
data/**
!data/.gitkeep
