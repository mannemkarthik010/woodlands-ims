# Copy to .env. NEVER commit the real one, and never paste these into Slack.
DJANGO_SECRET_KEY=change-me
DJANGO_DEBUG=1
DATABASE_URL=sqlite:///db.sqlite3

# Shift4 daily report mailbox (the CSV arrives here; read-only IMAP)
SHIFT4_REPORT_IMAP_HOST=
SHIFT4_REPORT_IMAP_USER=
SHIFT4_REPORT_IMAP_PASSWORD=
